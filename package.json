{
  "name": "sam-env-action",
  "version": "0.1.0",
  "private": true,
  "description": "Action to setup variables based on branch name",
  "main": "index.js",
  "scripts": {
    "test": "jest && eslint . && prettier --check .",
    "build": "ncc build index.js --out dist --license licenses.txt",
    "format": "prettier --write .",
    "all": "npm run format && npm test && npm run build"
  },
  "license": "Apache-2.0",
  "dependencies": {
    "@actions/core": "1.10.0",
    "@actions/github": "5.0.3"
  },
  "devDependencies": {
    "@vercel/ncc": "0.36.0",
    "eslint": "7.32.0",
    "jest": "27.5.1",
    "prettier": "2.7.1"
  }
}
